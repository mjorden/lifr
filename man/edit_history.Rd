% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{edit_history}
\alias{edit_history}
\alias{edit_history_save}
\alias{edit_history_clear}
\title{Inspect, save, or clear the edit history}
\usage{
edit_history(data)

edit_history_save(data, file)

edit_history_clear(data)
}
\arguments{
\item{data}{A data frame, typically one returned by an editor.}

\item{file}{Output CSV path for \code{edit_history_save()}.}
}
\value{
\code{edit_history()} returns the history data frame (empty, with a
message, when there is none). \code{edit_history_save()} returns \code{file}
invisibly. \code{edit_history_clear()} returns \code{data} without the attribute.
}
\description{
Every editor appends one row to the \code{edits} attribute of the frame it
returns: \code{timestamp}, \code{fn}, \code{boring} (\code{NA} = all borings), \code{top},
\code{bottom}, \code{value}, \code{n_rows_changed}, \code{notes}. These accessors read, write
out, or remove that audit trail.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif <- lif_editor_bulk(lif, top = 0, bottom = 2, verbose = FALSE)
edit_history(lif)
lif <- edit_history_clear(lif)
is.null(attr(lif, "edits"))
}
