% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{lif_edits_rescue}
\alias{lif_edits_rescue}
\alias{lif_get_edits}
\alias{lif_set_edits}
\title{Carry the edit history across dplyr operations}
\usage{
lif_get_edits(data)

lif_set_edits(data, edits)
}
\arguments{
\item{data}{A data frame.}

\item{edits}{The history returned by \code{lif_get_edits()}, or \code{NULL} to
clear it.}
}
\value{
\code{lif_get_edits()}: the history data frame or \code{NULL}.
\code{lif_set_edits()}: \code{data} with the history attached.
}
\description{
dplyr verbs strip non-standard attributes, which silently drops the
edit history. Save it with \code{lif_get_edits()} before such a step and put
it back with \code{lif_set_edits()} afterwards.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif <- lif_editor_bulk(lif, top = 0, bottom = 2, verbose = FALSE)
saved <- lif_get_edits(lif)
sub <- lif[lif$boring \%in\% c("LIF-01", "LIF-02"), ]
sub <- lif_set_edits(sub, saved)
nrow(edit_history(sub))
}
