% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{site_report}
\alias{site_report}
\title{Render the site report from a \code{lif_site} object}
\usage{
site_report(
  x,
  formats = c("html", "md"),
  output_dir = ".",
  file_prefix = NULL,
  quiet = FALSE
)
}
\arguments{
\item{x}{A \code{lif_site} object.}

\item{formats}{Any of \code{"html"}, \code{"md"}, \code{"pdf"}.}

\item{output_dir}{Directory to write into (created if missing).}

\item{file_prefix}{Base filename. Default: the prefix recorded by
\code{\link[=process_site]{process_site()}}, else a slug of the site name. Existing files are
overwritten.}

\item{quiet}{Suppress the "written" messages.}
}
\value{
Invisibly, a named list of the paths written (\code{html}, \code{md},
\code{pdf}, \code{logs_pdf} as applicable).
}
\description{
Renders the object returned by \code{\link[=process_site]{process_site()}} into any of:
\itemize{
\item \code{"html"}: one self-contained file (inline CSS/JS, base64 figures,
sortable tables) with four tabs: \strong{Summary} (tiles, boring maps, the
\%RE proxy disclosure, peak-response tables), \strong{Data & QA} (per-boring
inventory, summary statistics, site charts), \strong{Processing history}
(the complete edit log, corrections, pipeline steps), and \strong{Boring
logs} (input-vs-edited log pairs with a changed-only filter). Printing
shows every tab as its own section.
\item \code{"md"}: a Markdown document that references its figures as PNG
sidecars written next to it.
\item \code{"pdf"}: the Markdown rendered through \code{rmarkdown} (pandoc plus a LaTeX
distribution providing \code{xelatex}, such as TinyTeX). Skipped with a
warning when that toolchain is missing. Also writes \verb{<prefix>_logs.pdf}, one page per
boring, through R's own \code{pdf()} device, which needs no LaTeX.
}
}
\examples{
\donttest{
demo <- system.file("extdata", "demo", package = "lifr")
out <- tempfile("site")
site <- process_site(demo, output_dir = out, report = FALSE, quiet = TRUE)
site_report(site, formats = "md", output_dir = out, quiet = TRUE)
}
}
