% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{lif_zero_shallow}
\alias{lif_zero_shallow}
\title{Zero signal at or above a given depth}
\usage{
lif_zero_shallow(data, depth, borings = NULL, value = 0)
}
\arguments{
\item{data}{A \link{lif_data} frame (any data frame with \code{boring}, \code{depth},
\code{signal}).}

\item{depth}{Readings at or shallower than this depth (ft) are zeroed.}

\item{borings}{Character vector restricting the edit; \code{NULL} for all.}

\item{value}{Replacement value. Default 0; \code{NA} is allowed.}
}
\value{
The edited frame; one audit row per boring.
}
\description{
Convenience wrapper over \code{\link[=lif_editor]{lif_editor()}} that masks the top \code{depth} feet
of every boring (or the named \code{borings}), the usual way to remove
surface smear before summarising or mapping.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif <- lif_zero_shallow(lif, depth = 2)
}
