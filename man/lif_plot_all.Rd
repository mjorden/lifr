% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{lif_plot_all}
\alias{lif_plot_all}
\title{Plot LIF depth profiles for every boring}
\usage{
lif_plot_all(
  data,
  xmax = NULL,
  ymax = NULL,
  layout = c("facet", "list"),
  ncol = NULL,
  output_file = NULL,
  width = 12,
  height = 8,
  dpi = 200
)
}
\arguments{
\item{data}{A \link{lif_data} frame.}

\item{xmax}{Maximum depth shown (ft). Default: the deepest reading in
\code{data}, so every boring shares one depth axis.}

\item{ymax}{Signal axis cap shared by all borings. \code{NULL} uses 5\% above
the site maximum so panels are comparable.}

\item{layout}{\code{"facet"} (default) returns one ggplot with a small
multiple per boring; \code{"list"} returns a named list of single-boring
plots from \code{\link[=lif_plot]{lif_plot()}}.}

\item{ncol}{Facet columns for \code{layout = "facet"}; \code{NULL} lets ggplot2
choose.}

\item{output_file}{Optional PNG path for the facet plot.}

\item{width, height, dpi}{Passed to \code{ggplot2::ggsave()} when saving.}
}
\value{
A ggplot (facet) or a named list of ggplots (list), invisibly
when saved.
}
\description{
Plot LIF depth profiles for every boring
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif_plot_all(lif, ncol = 4)
}
