% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{lif_keep}
\alias{lif_keep}
\title{Keep one clean window for a boring and zero everything outside it}
\usage{
lif_keep(
  data,
  borename,
  top = -Inf,
  bottom = Inf,
  value = 0,
  keep = NULL,
  preview = FALSE
)
}
\arguments{
\item{data}{A \link{lif_data} frame (any data frame with \code{boring}, \code{depth},
\code{signal}).}

\item{borename}{Boring name to edit.}

\item{top, bottom}{Depth window (ft) to keep. Defaults \code{-Inf} / \code{Inf}; at
least one bound or \code{keep} is required.}

\item{value}{Replacement value. Default 0; \code{NA} is allowed.}

\item{keep}{Optional list of \code{c(top, bottom)} windows to keep together.}

\item{preview}{Logical. \code{TRUE} reports how many rows would change without
modifying the data.}
}
\value{
The edited frame with one audit row appended.
}
\description{
The inverse of \code{\link[=lif_editor]{lif_editor()}}: every reading of \code{borename} whose depth
falls outside the kept window(s) is set to \code{value}. One audit row is
recorded for the whole call, spanning the outermost kept depths, with the
window list in \code{notes} (\code{"kept 12-28 ft; zeroed rows outside these windows"}); the report's log plots read that note to shade the zeroed
depths.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif <- lif_keep(lif, "LIF-04", top = 10, bottom = 25)
lif <- lif_keep(lif, "LIF-06", keep = list(c(8, 12), c(20, 28)))
}
