% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{lif_plot}
\alias{lif_plot}
\title{Plot the LIF depth profile of one boring}
\usage{
lif_plot(data, borename, xmax = NULL, ymax = NULL)
}
\arguments{
\item{data}{A \link{lif_data} frame.}

\item{borename}{Boring to plot.}

\item{xmax}{Maximum depth shown (ft). Default: the deepest reading in
\code{data}, so every boring shares one depth axis.}

\item{ymax}{Signal axis cap. \code{NULL} (default) auto-scales to 5\% above the
boring's maximum.}
}
\value{
A ggplot object.
}
\description{
Signal against depth, filled with the instrument's emission colour at
each depth when a \code{color} channel is present (with a colour strip left of
zero), or a single fill otherwise.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif_plot(lif, "LIF-05")
lif_plot(lif, "LIF-05", ymax = 100)
}
