% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process.R
\name{re_feet}
\alias{re_feet}
\title{Integrate LIF signal over depth per boring (RE-feet)}
\usage{
re_feet(data)
}
\arguments{
\item{data}{A \link{lif_data} frame.}
}
\value{
One row per boring with \code{boring}, \code{re_feet}, \code{peak_signal},
\code{peak_depth}, and the boring's \code{easting}, \code{northing}, \code{msl} when
present.
}
\description{
The trapezoidal integral of \code{signal} against \code{depth} for each boring, a
single number summarising how much fluorescence a boring encountered.
Like \%RE itself it is a screening quantity, not a mass or concentration.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
re_feet(lif)
}
