% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process.R
\name{hp_correction}
\alias{hp_correction}
\title{Remove the hydrostatic component from hydraulic push pressure}
\usage{
hp_correction(data, gradient = 0.433, water_table = 0)
}
\arguments{
\item{data}{A data frame with \code{depth} and \code{hp} columns.}

\item{gradient}{Hydrostatic gradient in psi/ft. Default 0.433 (fresh
water).}

\item{water_table}{Depth to water (ft below ground surface): one value
applied site-wide, or a named vector of per-boring depths such as
\code{c("LIF-01" = 12, "LIF-02" = 14.5)}. Default 0 applies the correction
from the ground surface and prints a reminder to supply a real value.}
}
\value{
\code{data} with \code{hp} corrected in place and one audit row recording
the parameters.
}
\description{
Raw \code{hp} rises with depth from the weight of the water column alone.
Subtracting \code{gradient * max(0, depth - water_table)} leaves the pressure
the formation itself exerts, which is the quantity that reflects
permeability.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif <- hp_correction(lif, water_table = 6)
edit_history(lif)
}
