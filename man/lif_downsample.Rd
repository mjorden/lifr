% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process.R
\name{lif_downsample}
\alias{lif_downsample}
\title{Thin a LIF frame to a coarser depth spacing}
\usage{
lif_downsample(data, target_interval = 1, preserve_above = NULL)
}
\arguments{
\item{data}{A \link{lif_data} frame.}

\item{target_interval}{Target spacing (ft). Default 1.}

\item{preserve_above}{Signal threshold above which every reading is kept.
\code{NULL} (default) thins uniformly.}
}
\value{
The thinned frame, sorted by depth within each boring.
}
\description{
Keeps one reading every \code{target_interval} feet per boring, always
retaining readings whose \code{signal} exceeds \code{preserve_above} so peaks are
never thinned away. Row deletion is an edit: one audit row records the
parameters and the number of rows removed.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
thin <- lif_downsample(lif, target_interval = 1, preserve_above = 5)
nrow(lif); nrow(thin)
}
