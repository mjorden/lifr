% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{lif_editor}
\alias{lif_editor}
\title{Zero LIF signal inside a depth window for one boring}
\usage{
lif_editor(
  data,
  borename,
  top = 0,
  bottom = 1000,
  value = 0,
  delete = NULL,
  preview = FALSE
)
}
\arguments{
\item{data}{A \link{lif_data} frame (any data frame with \code{boring}, \code{depth},
\code{signal}).}

\item{borename}{Boring name to edit.}

\item{top, bottom}{Depth interval (ft) to zero. Defaults \code{0} and \code{1000}
cover the whole boring, which triggers a warning.}

\item{value}{Replacement value. Default 0; \code{NA} is allowed.}

\item{delete}{Optional list of \code{c(top, bottom)} pairs to zero several
intervals in one call. Cannot be combined with \code{top}/\code{bottom}.}

\item{preview}{Logical. \code{TRUE} reports how many rows would change without
modifying the data.}
}
\value{
The edited frame with one audit row per interval appended to its
edit history (see \code{\link[=edit_history]{edit_history()}}). With \code{preview = TRUE} the input is
returned unchanged.
}
\description{
Sets \code{signal} to \code{value} (default 0) for every reading of \code{borename}
whose depth falls inside the interval(s). Use this to mask known
artifacts: surface smear, a rod change, a fluorescent mineral seam. The
inverse, keeping one clean window and zeroing everything else, is
\code{\link[=lif_keep]{lif_keep()}}.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif <- lif_editor(lif, "LIF-03", top = 0, bottom = 2)
lif <- lif_editor(lif, "LIF-05", delete = list(c(0, 1.5), c(30, Inf)))
edit_history(lif)
}
