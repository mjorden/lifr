% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{lif_editor_bulk}
\alias{lif_editor_bulk}
\title{Zero LIF signal inside a depth window across all borings}
\usage{
lif_editor_bulk(
  data,
  top = 0,
  bottom = 1000,
  value = 0,
  preview = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \link{lif_data} frame (any data frame with \code{boring}, \code{depth},
\code{signal}).}

\item{top, bottom}{Depth interval (ft) to zero. Defaults \code{0} and \code{1000}
cover the whole boring, which triggers a warning.}

\item{value}{Replacement value. Default 0; \code{NA} is allowed.}

\item{preview}{Logical. \code{TRUE} reports how many rows would change without
modifying the data.}

\item{verbose}{Logical. Print a per-boring breakdown of rows changed.}
}
\value{
The edited frame with one audit row (boring \code{NA}, meaning all
borings) appended.
}
\description{
Zero LIF signal inside a depth window across all borings
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif <- lif_editor_bulk(lif, top = 0, bottom = 2)
lif_editor_bulk(lif, top = 0, bottom = 1, preview = TRUE)
}
