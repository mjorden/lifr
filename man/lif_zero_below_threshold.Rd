% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{lif_zero_below_threshold}
\alias{lif_zero_below_threshold}
\title{Zero signal below a threshold value}
\usage{
lif_zero_below_threshold(data, threshold, borings = NULL, value = 0)
}
\arguments{
\item{data}{A \link{lif_data} frame (any data frame with \code{boring}, \code{depth},
\code{signal}).}

\item{threshold}{Readings below this \%RE value are zeroed.}

\item{borings}{Character vector restricting the edit; \code{NULL} for all.}

\item{value}{Replacement value. Default 0; \code{NA} is allowed.}
}
\value{
The edited frame with one audit row recording the threshold.
}
\description{
Sets \code{signal} to \code{value} wherever it is strictly less than \code{threshold},
across all borings or the named subset. \code{NA} readings are left alone.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif <- lif_zero_below_threshold(lif, threshold = 1)
}
