% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize_lif}
\alias{summarize_lif}
\title{Summarise a LIF channel for QA reporting}
\usage{
summarize_lif(
  data,
  channel = "signal",
  thresholds = NULL,
  depth_bins = c(0, 5, 10, 15, 20, Inf),
  quantiles = c(0.05, 0.25, 0.5, 0.75, 0.95),
  detection_limit = 0,
  sort_by = c("peak", "input"),
  plot = FALSE
)
}
\arguments{
\item{data}{A \link{lif_data} frame.}

\item{channel}{Column to summarise. Default \code{"signal"}; \code{"ec"} and \code{"hp"}
also work.}

\item{thresholds}{Threshold values for \verb{$by_threshold}. \code{NULL} uses a
unit-appropriate default (\code{c(1, 2, 5, 10, 50)} \%RE for signal).}

\item{depth_bins}{Break points (ft) for \verb{$by_depth_zone}; the last may be
\code{Inf}.}

\item{quantiles}{Probabilities for \verb{$quantiles}.}

\item{detection_limit}{Minimum value counted as a detection in
\code{pct_detect}. Default 0.}

\item{sort_by}{Order of \verb{$by_boring}: \code{"peak"} (descending, default) or
\code{"input"} (first-appearance order).}

\item{plot}{Logical. Also print a lollipop chart of peak signal per boring
(see \code{\link[=chart_peak_by_boring]{chart_peak_by_boring()}}).}
}
\value{
A list: \code{overview} (one row: \code{n_borings}, \code{n_samples}, \code{min},
\code{mean}, \code{geom_mean}, \code{geom_mean_n_excluded}, \code{median}, \code{max},
\code{pct_detect}), \code{by_boring} (\code{boring}, \code{n}, \code{peak}, \code{peak_depth},
\code{mean}, \code{geom_mean}, \code{geom_mean_n_excluded}, \code{p50}, \code{p95}),
\code{by_threshold} (\code{threshold}, \code{units}, \code{n_samples_above},
\code{pct_samples_above}, \code{n_borings_above}, \code{pct_borings_above}),
\code{by_depth_zone} (\code{depth_zone}, \code{n}, \code{pct_detect}, \code{p50}, \code{p95}), and
\code{quantiles} (one row, \code{channel} plus one column per probability).
}
\description{
Standard site-level, per-boring, per-threshold, and per-depth-zone
statistics for \code{signal} (or another numeric channel), returned as a list
of data frames so reports and dashboards render the same numbers.
}
\section{Geometric mean}{

LIF response is right-skewed, so the geometric mean is reported beside
the arithmetic mean. It is defined over positive values only;
\code{geom_mean_n_excluded} counts the zeros and negatives left out.
}

\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
qa <- summarize_lif(lif)
qa$overview
qa$by_boring
qa$by_threshold
}
