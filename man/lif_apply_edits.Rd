% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{lif_apply_edits}
\alias{lif_apply_edits}
\title{Apply a batch of edits from a data frame or CSV}
\usage{
lif_apply_edits(data, edits, validate = FALSE)
}
\arguments{
\item{data}{A \link{lif_data} frame.}

\item{edits}{A data frame or the path to a CSV with the columns above.}

\item{validate}{Logical. \code{TRUE} checks every row and prints how many
readings each would affect without modifying the data; invalid rows
stop with a message naming them.}
}
\value{
The edited frame (or, with \code{validate = TRUE}, the input
unchanged, invisibly).
}
\description{
Applies each row of an edit plan in order through \code{\link[=lif_editor]{lif_editor()}},
\code{\link[=lif_editor_bulk]{lif_editor_bulk()}}, or \code{\link[=lif_keep]{lif_keep()}}, so a site's edits live in one
reviewable file instead of a script of editor calls.
}
\section{Plan columns}{

\itemize{
\item \code{boring} (required): boring name, or \code{"*"} for every boring (delete
rows only; recorded as one bulk edit).
\item \code{action} (optional): \code{"delete"} (default), \code{"clean"} (alias), or
\code{"keep"}.
\item \code{top}, \code{bottom} (optional): depth bounds. \code{NA} means the function's own
default (whole boring for delete; open-ended for keep).
}
}

\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
plan <- data.frame(boring = c("*", "LIF-02", "LIF-05"),
                   action = c("delete", "delete", "keep"),
                   top = c(0, 25, 8), bottom = c(1.5, NA, 30))
lif_apply_edits(lif, plan, validate = TRUE)
lif <- lif_apply_edits(lif, plan)
edit_history(lif)
}
