% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basemap.R
\name{fetch_basemap}
\alias{fetch_basemap}
\title{Fetch public-domain aerial imagery for a site}
\usage{
fetch_basemap(data, crs, pad_frac = 0.3, px = 1024L, timeout = 30)
}
\arguments{
\item{data}{A data frame with \code{easting} and \code{northing} in projected
coordinates.}

\item{crs}{The projected coordinate reference system of the coordinates:
an EPSG code or anything \code{sf::st_crs()} accepts (for example \code{3452},
NAD83 Louisiana South ftUS). A geographic (lon/lat) CRS is rejected.}

\item{pad_frac}{Padding added around the site extent on each side, as a
fraction of the extent. Default 0.30.}

\item{px}{Pixel width of the tile; height follows the aspect ratio.}

\item{timeout}{Seconds allowed for the download.}
}
\value{
A \code{lifr_basemap} list (\code{image}, \code{xmin}, \code{xmax}, \code{ymin}, \code{ymax},
\code{attribution}) or \code{NULL}.
}
\description{
Downloads a USGS National Map imagery tile covering the boring cluster
(padded) for use as a \code{\link[=boring_map]{boring_map()}} underlay. Needs the \code{sf} and \code{png}
packages, network access, and a site inside US coverage; any failure
returns \code{NULL} with a warning so callers degrade gracefully.
}
\examples{
\dontrun{
bm <- fetch_basemap(lif, crs = 3452)
boring_map(lif, basemap = bm)
}
}
