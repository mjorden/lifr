% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{process_site}
\alias{process_site}
\title{Run the import -> edit -> correct -> summarise -> report pipeline for a site}
\usage{
process_site(
  data_dir,
  locations_file = NULL,
  edits = NULL,
  corrections = list(),
  output_dir = ".",
  prefix = NULL,
  site_name = NULL,
  crs = NULL,
  charts = TRUE,
  report = TRUE,
  report_formats = c("html", "md", "pdf"),
  response_thresholds = c(1, 10, 50),
  date_suffix = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{data_dir}{Directory of \code{.lif.dat.txt} files (and, usually, the
locations file).}

\item{locations_file}{Passed to \code{\link[=lif_import]{lif_import()}}.}

\item{edits}{Optional batch edit plan: a CSV path or data frame for
\code{\link[=lif_apply_edits]{lif_apply_edits()}}.}

\item{corrections}{Named list of corrections applied after the edits, in
this order:
\itemize{
\item \code{zero_shallow}: a depth (ft); readings at or above it are zeroed via
\code{\link[=lif_zero_shallow]{lif_zero_shallow()}}.
\item \code{zero_below}: a \%RE threshold; readings below it are zeroed via
\code{\link[=lif_zero_below_threshold]{lif_zero_below_threshold()}}.
\item \code{hp}: \code{TRUE} for \code{\link[=hp_correction]{hp_correction()}} defaults, or a named list of its
arguments such as \code{list(water_table = 8)}.
}}

\item{output_dir}{Directory for the charts and report (created if
missing).}

\item{prefix}{Filename prefix for the charts and report. Default: a slug
of \code{site_name}.}

\item{site_name}{Human-readable site name. Default: the \code{data_dir}
basename.}

\item{crs}{Projected CRS (EPSG code) of the coordinates. When supplied
the report adds a boring map over USGS aerial imagery via
\code{\link[=fetch_basemap]{fetch_basemap()}}; needs network access and degrades to a note offline.}

\item{charts}{Logical. Write the site summary charts as PNGs.}

\item{report}{Logical. Render the report.}

\item{report_formats}{Any of \code{"html"}, \code{"md"}, \code{"pdf"}.}

\item{response_thresholds}{The Summary tab lists the borings whose peak
exceeds each of these \%RE values. \code{NULL} suppresses those tables.}

\item{date_suffix}{Logical. Append \verb{_YYMMDD} to the prefix so successive
runs sit side by side.}

\item{quiet}{Suppress progress messages.}
}
\value{
Invisibly, a \code{lif_site} object: a list with \code{data} (processed
frame), \code{raw_data} (as imported), \code{site_name}, \code{log} (one row per step),
\code{summary} (\code{\link[=summarize_lif]{summarize_lif()}} result), \code{edits} (the audit trail),
\code{charts} (PNG paths), \code{reports} (named list of report paths), and
\code{meta}.
}
\description{
One call that imports a directory of LIF logs, optionally applies a batch
edit plan and corrections, summarises the data, writes the site summary
charts, and renders the site report. Every step is logged and every edit
is recorded in the audit trail the report prints.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
out <- tempfile("site")
site <- process_site(demo, output_dir = out,
                     corrections = list(zero_shallow = 1),
                     report = FALSE, quiet = TRUE)
site
site$log
\donttest{
site <- process_site(demo, output_dir = out, report_formats = "html",
                     quiet = TRUE)
site$reports$html
}
}
\seealso{
\code{\link[=site_report]{site_report()}} to re-render from the returned object.
}
