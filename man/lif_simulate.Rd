% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{lif_simulate}
\alias{lif_simulate}
\title{Simulate a synthetic LIF site}
\usage{
lif_simulate(
  n_borings = 12,
  dir = NULL,
  seed = 1,
  extent = 200,
  origin = c(1000, 2000),
  interval = 0.25,
  depth_range = c(22, 40),
  lobes = NULL,
  msl = 150,
  noise = 0.25,
  verbose = TRUE
)
}
\arguments{
\item{n_borings}{Number of borings. Default 12.}

\item{dir}{Directory to write into (created if missing). \code{NULL} returns
the data frame without writing.}

\item{seed}{Random seed. Default 1.}

\item{extent}{Site width and height (ft). Default 200.}

\item{origin}{\code{c(easting, northing)} of the site's lower-left corner.}

\item{interval}{Depth sampling interval (ft). Default 0.25.}

\item{depth_range}{Range of boring total depths (ft), sampled uniformly.}

\item{lobes}{A list of plume lobes, each a list with \code{e}, \code{n} (plan
position, ft), \code{elev} (core elevation, ft MSL), \code{sig_h}, \code{sig_v}
(horizontal and vertical spread, ft), and \code{peak} (\%RE). \code{NULL} uses two
overlapping lobes near the centre of the site.}

\item{msl}{Mean ground-surface elevation (ft MSL). Default 150.}

\item{noise}{Log-normal noise standard deviation on the signal.}

\item{verbose}{Print the written file names.}
}
\value{
With \code{dir = NULL}, a \link{lif_data} frame (with the locations joined).
Otherwise the directory path, invisibly.
}
\description{
Generates plausible LIF logs for a made-up site: irregular boring
positions over a rectangular area, a plume built from one or more
three-dimensional Gaussian lobes with log-normal noise, a hydrostatic HP
pressure profile with thin low-pressure sand layers, an EC profile that
rises inside the plume and drops in the sands, and an emission colour
keyed to signal strength. Written as one \code{.lif.dat.txt} per boring plus a
\code{locations.csv}, exactly the layout \code{\link[=lif_import]{lif_import()}} reads, or returned as a
\link{lif_data} frame when \code{dir} is \code{NULL}. The data has no relation to any real
site.
}
\examples{
lif <- lif_simulate(n_borings = 6)
lif
boring_map(lif)

d <- tempfile("site")
lif_simulate(n_borings = 4, dir = d, verbose = FALSE)
list.files(d)
}
