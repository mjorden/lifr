% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{lif_overview}
\alias{lif_overview}
\title{Multi-channel overview of one boring}
\usage{
lif_overview(
  data,
  borename,
  channels = c("ec", "hp"),
  depth_max = NULL,
  ymax = NULL
)
}
\arguments{
\item{data}{A \link{lif_data} frame.}

\item{borename}{Boring to plot.}

\item{channels}{Channels to draw after the LIF panel, in order. Default
\code{c("ec", "hp")}; channels absent or all-\code{NA} for the boring are
skipped.}

\item{depth_max}{Shared depth limit (ft). Default: the boring's deepest
reading.}

\item{ymax}{Signal axis cap. \code{NULL} (default) auto-scales to 5\% above the
boring's maximum.}
}
\value{
A patchwork object (prints like a ggplot).
}
\description{
LIF signal beside whichever of \code{ec} and \code{hp} the boring recorded, on a
shared depth axis.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif_overview(lif, "LIF-05")
}
