% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{lif_read}
\alias{lif_read}
\title{Read one raw LIF log file}
\usage{
lif_read(file, legacy = FALSE)
}
\arguments{
\item{file}{Path to one log file.}

\item{legacy}{Logical. \code{TRUE} for older firmware exports that have no
header row: an 11-column file is read as a LIF log and a 16-column file
as a combined HP-LIF log. Default \code{FALSE} (header row present).}
}
\value{
A data frame with \code{boring}, \code{depth}, \code{signal}, then the optional
canonical channels present in the file, then every remaining source
column.
}
\description{
Reads a single \code{.lif.dat.txt} (or \code{.csv}) log into a data frame, keeping
every column the instrument recorded. The known channels are renamed to
\code{depth}, \code{signal}, \code{ec}, \code{hp}, and \code{color}; any other column is kept under
a cleaned snake_case version of its header (for example \code{EC.Depth}
becomes \code{ec_depth}, \verb{Detector 1 Max (uV)} becomes \code{detector_1_max_uv}).
The boring name is the file name with its \verb{.lif.dat.*} suffix removed.
}
\examples{
f <- system.file("extdata", "demo", "LIF-01.lif.dat.txt", package = "lifr")
head(lif_read(f))
}
\seealso{
\code{\link[=lif_import]{lif_import()}} for a whole directory with a locations join.
}
