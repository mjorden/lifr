% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts.R
\name{export_site_charts}
\alias{export_site_charts}
\title{Write the site summary charts as PNG files}
\usage{
export_site_charts(
  data,
  output_dir = ".",
  prefix = "site",
  thresholds = c(1, 5),
  width = 7,
  height = 3.4,
  dpi = 150,
  quiet = FALSE
)
}
\arguments{
\item{data}{A \link{lif_data} frame.}

\item{output_dir}{Directory to write into (created if missing).}

\item{prefix}{Filename prefix. Default \code{"site"}.}

\item{thresholds}{Passed to \code{\link[=chart_max_response]{chart_max_response()}}.}

\item{width, height, dpi}{\code{ggsave()} geometry.}

\item{quiet}{Suppress the per-file message.}
}
\value{
Character vector of written paths, invisibly.
}
\description{
Writes whichever of \code{\link[=chart_max_response]{chart_max_response()}}, \code{\link[=chart_peak_by_boring]{chart_peak_by_boring()}},
\code{\link[=chart_hp_histogram]{chart_hp_histogram()}}, and \code{\link[=chart_ec_histogram]{chart_ec_histogram()}} the data supports as
\verb{<prefix>_chart_<name>.png}.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
out <- tempfile("charts")
export_site_charts(lif, out, quiet = TRUE)
list.files(out)
}
