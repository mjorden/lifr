% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{depth_slice_map}
\alias{depth_slice_map}
\title{Plan-view map of aggregated signal in a depth band}
\usage{
depth_slice_map(
  data,
  depth_range,
  channel = "signal",
  agg_fn = max,
  point_size = 3,
  label_borings = TRUE,
  show_unsampled = TRUE,
  title = NULL,
  coord_units = "ft",
  seed = 42
)
}
\arguments{
\item{data}{A \link{lif_data} frame with \code{easting} and \code{northing}.}

\item{depth_range}{\code{c(top, bottom)} in ft.}

\item{channel}{Column to aggregate. Default \code{"signal"}.}

\item{agg_fn}{Aggregation function applied per boring. Default \code{max}.}

\item{point_size}{Marker size. Default 3.}

\item{label_borings}{Logical. Label each boring.}

\item{show_unsampled}{Logical. Mark borings with no in-band readings.}

\item{title}{Plot title. \code{NULL} builds one from the channel and depth
band.}

\item{coord_units}{Unit string for the axis titles and scale bar.}

\item{seed}{Seed for reproducible label placement.}
}
\value{
A ggplot object.
}
\description{
Each boring is coloured by the aggregate (default maximum) of \code{signal}
inside \code{depth_range}. Borings with no usable readings in the band are
drawn as grey crosses and named in a message, so "not sampled here" is
never mistaken for "no boring here".
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
depth_slice_map(lif, depth_range = c(10, 20))
}
