% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{lif_data}
\alias{lif_data}
\alias{new_lif_data}
\alias{print.lif_data}
\alias{summary.lif_data}
\title{Construct a validated LIF data frame}
\usage{
new_lif_data(df)

\method{print}{lif_data}(x, ..., n = 10)

\method{summary}{lif_data}(object, ...)
}
\arguments{
\item{df}{A data frame with at least \code{boring}, \code{depth}, \code{signal}.}

\item{x}{A \code{lif_data} object.}

\item{...}{Ignored.}

\item{n}{Number of rows to preview. Default 10.}

\item{object}{A \code{lif_data} object.}
}
\value{
\code{df} with class \code{lif_data} prepended. Construction is idempotent.

\code{print()} returns \code{x} invisibly.

\code{summary()} returns the list from \code{\link[=summarize_lif]{summarize_lif()}}.
}
\description{
Tags a data frame with the \code{lif_data} class after checking that it carries
\code{boring}, \code{depth}, and \code{signal}. The object still inherits from
\code{data.frame}, so every base-R and tidyverse operation keeps working; the
class only adds \code{print()} and \code{summary()} methods. Importers return
classed objects automatically.
}
\examples{
df <- data.frame(boring = "B1", depth = 1:3, signal = c(0, 5, 2))
lif <- new_lif_data(df)
class(lif)
}
