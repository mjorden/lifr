% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{lif_import}
\alias{lif_import}
\title{Import a directory of LIF logs and join boring locations}
\usage{
lif_import(
  data_dir = ".",
  locations_file = NULL,
  pattern = .LIF_FILE_PATTERN,
  legacy = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data_dir}{Directory containing the log files.}

\item{locations_file}{Path to the locations file (see \code{\link[=read_locations]{read_locations()}}).
\code{NULL} (default) looks for \code{locations*.csv} / \code{locations*.txt} inside
\code{data_dir}, preferring \code{locations.csv}; when none is found the logs are
imported without coordinates and a message says so. \code{FALSE} skips the
join explicitly.}

\item{pattern}{Regular expression selecting the log files. Default matches
\code{.lif.dat.txt} and \code{.lif.dat.csv}.}

\item{legacy}{Passed to \code{\link[=lif_read]{lif_read()}}: \code{TRUE} for headerless legacy files.}

\item{verbose}{Logical. Print per-file progress and the import summary.}
}
\value{
A \link{lif_data} object. Columns are ordered \code{easting}, \code{northing},
\code{depth}, \code{signal}, \code{boring}, \code{msl}, \code{ec}, \code{hp}, \code{color} (those present),
then every other source column. Borings that have a log file but no row
in the locations file are dropped with a warning naming them; if no
boring matches at all the import stops, because that is almost always a
naming-format mismatch.
}
\description{
Reads every \verb{*.lif.dat.txt} / \verb{*.lif.dat.csv} file in \code{data_dir} with
\code{\link[=lif_read]{lif_read()}}, binds them into one data frame (one row per depth reading),
and joins a locations table so every row carries its boring's \code{easting},
\code{northing}, and ground-surface elevation \code{msl}. All source columns are
preserved.
}
\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif
unique(lif$boring)
}
