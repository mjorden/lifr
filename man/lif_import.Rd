% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{lif_import}
\alias{lif_import}
\title{Import a directory of LIF logs and join boring locations}
\usage{
lif_import(
  data_dir = ".",
  locations_file = NULL,
  pattern = .LIF_FILE_PATTERN,
  legacy = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data_dir}{Directory containing the log files.}

\item{locations_file}{Path to the locations file (see \code{\link[=read_locations]{read_locations()}}).
\code{NULL} (default) looks for \code{locations*.csv} / \code{locations*.txt} inside
\code{data_dir}, preferring \code{locations.csv}; when none is found the logs are
imported without coordinates and a message says so. \code{FALSE} skips the
join explicitly.}

\item{pattern}{Regular expression selecting the log files. Default matches
\code{.lif.dat.txt} and \code{.lif.dat.csv}.}

\item{legacy}{Passed to \code{\link[=lif_read]{lif_read()}}: \code{TRUE} for headerless legacy files.}

\item{verbose}{Logical. Print per-file progress and the import summary.}
}
\value{
A \link{lif_data} object. Columns are ordered \code{easting}, \code{northing},
\code{depth}, \code{signal}, \code{boring}, \code{msl}, \code{ec}, \code{hp}, \code{color} (those present),
then every other source column. Borings that have a log file but no row
in the locations file are dropped with a warning naming them; if no
boring matches at all the import stops, because that is almost always a
naming-format mismatch.
}
\description{
Reads every \verb{*.lif.dat.txt} / \verb{*.lif.dat.csv} file in \code{data_dir} with
\code{\link[=lif_read]{lif_read()}}, binds them into one data frame (one row per depth reading),
and joins a locations table so every row carries its boring's \code{easting},
\code{northing}, and ground-surface elevation \code{msl}. All source columns are
preserved.
}
\section{The survey folder}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{MySite/LIF/
  LIF-01.lif.dat.txt    one tab-delimited log per boring, header row
  LIF-02.lif.dat.txt
  ...
  locations.csv         boring, easting, northing, msl
}\if{html}{\out{</div>}}

The boring name is the file name without its \code{.lif.dat.txt} suffix and
must match the \code{boring} column of the locations file exactly.
}

\section{Column handling}{

Instrument headers vary between probes and firmware versions. These are
recognised (case and punctuation ignored) and renamed:
\code{Depth} to \code{depth}; \code{Signal} to \code{signal}; \code{EC.Value} / \code{EC} to \code{ec};
\code{HP.PresDown} / \code{HP} to \code{hp}; \code{color} / \code{Color} to \code{color} (normalised to
\verb{#RRGGBB}). Any other column is kept under a cleaned snake_case name, so
\code{EC.Depth} becomes \code{ec_depth} and \verb{Detector 1 Max (uV)} becomes
\code{detector_1_max_uv}. Files that disagree on their column set are bound
with \code{NA} in the missing cells and a warning naming them.
}

\examples{
demo <- system.file("extdata", "demo", package = "lifr")
lif <- lif_import(demo, verbose = FALSE)
lif
unique(lif$boring)
names(lif)

# Logs only, before the coordinates arrive
logs <- lif_import(demo, locations_file = FALSE, verbose = FALSE)
"easting" \%in\% names(logs)

\dontrun{
# A project folder with an explicit locations table
lif <- lif_import("C:/Projects/MySite/LIF",
                  locations_file = "C:/Projects/MySite/survey/borings.csv")

# Older headerless firmware exports
lif <- lif_import("C:/Projects/OldSite/LIF", legacy = TRUE)
}
}
\seealso{
\code{vignette("data-format")} for the file formats in detail.
}
